import { MatIconRegistry } from '@angular/material';
import { DomSanitizer } from '@angular/platform-browser';
export interface IconsConfig {
    keys: string[];
    path: string;
}
export declare class IconImportService {
    private iconRegistry;
    private domSanitizer;
    icons: IconsConfig;
    constructor(iconRegistry: MatIconRegistry, domSanitizer: DomSanitizer, icons: IconsConfig);
    register(): void;
}
